# Permit the deployment account to refresh only the strictly validated DuckDNS
# credentials. The helper is root-owned by deploy/setup-github-actions.sh.
rocky ALL=(root) NOPASSWD: /usr/local/sbin/bora-update-duckdns-env
